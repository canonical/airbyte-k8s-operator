name: Tests

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
    with:
      with-uv: true
      self-hosted-runner: false
