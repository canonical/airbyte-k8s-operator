name: Tests

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
